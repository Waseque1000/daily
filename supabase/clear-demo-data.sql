-- Optional: run in Supabase SQL Editor if you previously inserted demo rows manually.
-- Deletes all tasks and reminders (keeps your account and preferences).

-- delete from public.focus_sessions;
-- delete from public.reminders;
-- delete from public.tasks;
